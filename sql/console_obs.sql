CREATE TABLE console_obs (
    time TIMESTAMPTZ NOT NULL,
    tempinf REAL,
    humidityin REAL,
    PRIMARY KEY (time)
);



CREATE TABLE console_obs (time TIMESTAMPTZ NOT NULL, tempinf REAL, humidityin REAL, PRIMARY KEY (time));